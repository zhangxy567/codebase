1写操作只有第一次打开情况 清空
2一个数据流在一个fopen和fclose操作的位置是迭代+的,fputs写数据都是接着后面
3fgets 函数参数3个,满足num最大值就结束,且只读一行,末尾容许的话是先放/n,再放/0
4读操作总是配合while使用原因:都是先读并且同时做判断;
5标准输出输入也是流的一种,也就是流指针可以用stdin和stdout;

6sprintf和sscanf是把格式化数据和字符串互转

sprintf  是把格式化数据转换成字符串;即把格式化数据字符串式写
sscanf  是把字符串转换成格式化数据;即对字符串格式化的读