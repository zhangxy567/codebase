#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdbool.h>
#include <math.h>
#include <time.h>


#include <iostream>
using namespace std;
//
//前置++:	++变量名	先把变量的值加1，然后再执行表达式。
//后置++:	变量名++	先执行表达式，再把变量的值加1。
//前置--:	--变量名	先把变量的值减1，然后再执行表达式。
//后置--:	变量名--	先执行表达式，再把变量的减1。
//
// 在非表达式内部,i++或++i作为单独语句,则效果相同;
// 在表达式内部需要具体情况具体分析
// 
// 自增自减运算符可以应用到C++任何语句
// 
//
int main()
{
	return 0;
}