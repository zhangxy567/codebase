#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdbool.h>
#include <math.h>
#include <time.h>


#include <iostream>
using namespace std;
//在C++中,把表达式中的变量称为操作数
// c = a				//把右边操作数的值赋给左边操作数。
// c += a               //相当于 c = c + a;
// c -= a				//相当于 c = c - a;
// c /= a				//相当于c = c / a;
// c *= a				//相当于 c = c * a;
// c %= a				//相当于 c = c % a;
// 注意:
// (1)字符串（string）只能使用等号（=）赋值，不能使用其它的赋值运算符；使用其他运算符类似于字符串拼接
// (2)浮点数不能使用%=运算符；
// (3)等号（=）赋值运算符可以连续使用；遵循运算符优先级右结合性
// (4)如果赋值运算符两边操作数的数据类型不同，C++将自动转换数据类型，可能会损失精度，也可能超出取值范围，如果转换不成功，编译时会报错。
// 
// 
// 
//
int main()
{
	//int a = 100;
	//int c = 200;
	//a += c;
	//cout << a << endl;
	//int a = 100;
	//float c = 3.14;
	//a = c;
	//cout << a << endl;
	return 0;
}
