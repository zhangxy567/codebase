#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdbool.h>
#include <math.h>
#include <time.h>


#include <iostream>
using namespace std;
//if语句补充:
//(1)if (表达式)之间的空格可以不写。
//(2)表达式必须使用括号。
//(3)表达式可以是具体的值。
//(4)表达式一般是关系运算和逻辑运算表达式，也可以是赋值运算或其它的表达式。
//(5)不要把if（变量名 == 值）写成if（变量名 = 值）。
//(6)整个if语句可以没有else分支。
//(7)if或else分支花括号内的代码如果只有一行，花括号可以不写；如果一行也没有，花括号一定要写；如果有多行，花括号一定要写，如果不写，除了第一行，其它的代码将不是if语句的一部分。
//(8)if (表达式)和else后不能加分号，因为分号表示空语句。（C++用分号作为一条语句结束的标志。如果在C++程序中不小心多写了一个分号，那么该分号会被当成一条空语句。）
//(9)多条件的if语句本质上是嵌套的if语句。
//(10)最多只能有127个条件分支。
//(11)最后一个else可以没有。

// 
// 
// 
//
int main()
{
	return 0;
}